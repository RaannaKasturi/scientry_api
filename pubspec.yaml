name: scientry_api
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.8.1

dependencies:
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^3.2.0
  dio: ^5.8.0+1
  isolate_pool_executor: ^2.1.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  uuid: ^4.5.1

dev_dependencies:
  http: ^1.1.0
  lints: ^5.0.0
  test: ^1.24.0
